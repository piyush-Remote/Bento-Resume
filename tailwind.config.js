module.exports = {
  content: [
    './index.html', // Main HTML file, adjust path if needed
    './src/**/*.{js,ts,jsx,tsx}', // Make sure this is correct for React (JSX/TSX files)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
